/**
 * Creating history for the application
 * It enables pushing or redirecting to another route
 * Example after logging in, the user is directed to dashboard
 */

import { createBrowserHistory } from 'history';

export const history = createBrowserHistory();
